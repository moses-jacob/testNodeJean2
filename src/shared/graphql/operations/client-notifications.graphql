query ClientNotifications($filter: ClientNotificationFilter) {
  clientNotifications(filter: $filter) {
    edges {
      node {
        startDate
        endDate
        notice
        isActive
      }
    }
  }
}
