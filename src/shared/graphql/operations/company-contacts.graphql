query CompanyContacts($filter: CompanyContactFilter!) {
  companyContacts(filter: $filter) {
    edges {
      node {
        email
        portalPreferences {
          preferences
        }
      }
    }
  }
}
